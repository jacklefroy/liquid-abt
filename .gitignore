# LIQUID ABT - Minimal .gitignore for Developer Review
# Only excludes truly sensitive production data

# Build outputs and dependencies (standard exclusions)
node_modules/
.next/
/out/
*.tsbuildinfo

# Development tools
.DS_Store
.vscode/
.idea/

# Only exclude genuinely sensitive files:

# Real production credentials (if any exist)
.env.production
.env.prod
*prod*.key
*production*.key

# Real private keys and wallets (if any exist)  
wallet.dat
*.wallet
*private-key-prod*
*mainnet*.key

# Logs that might contain sensitive data
*.log
logs/

# Temporary and cache files
.cache/
*.tmp
*.temp

# Test coverage reports
/coverage

# Package manager debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*